element(3, {abc, def, ghi, jkl}).
setelement(3, {abc, def, ghi, jkl}, mno).

{abc, def}=={abc, def}.
{abc, def}=={abc, ghi}.

"Hello"==[72,101,108,108,111].

[$H, $e, $l, $l, $o].

c(tut).
tut:double(10).

c(tut1).
tut1:fac(10).
tut1:mult(3,4).

c(tut2).
tut2:convert(3, inch).
tut2:convert(10, centimeter).

halt().

