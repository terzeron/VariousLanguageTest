(buffer-name)
(buffer-file-name)

(current-buffer) ; the real current buffer, instead of its name
(other-buffer)

(switch-to-buffer (other-buffer))
(switch-to-buffer (other-buffer (current-buffer) t))

(buffer-size)

(point)

(point)

(buffer-size)